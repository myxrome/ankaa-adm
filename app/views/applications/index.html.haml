%h1 Applications
%table
  %thead
    %tr
      %th Name
      %th Key
      %th
      %th
  %tbody
    - @applications.each do |application|
      %tr
        %td= link_to application.name, application
        %td= application.key
        %td= link_to '...', edit_application_path(application), class: 'secondary-button'
        %td= link_to 'X', application, method: :delete, data: { confirm: 'Are you sure?' }, class: 'secondary-button'
= link_to 'New Application', new_application_path, class: 'main-button'

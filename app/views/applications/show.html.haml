%p#notice= notice
- content_for :title, "Application - #{@application.name}"
.row
  .small-4.large-4.columns
    %p Categories: #{@application.categories.map { |c| link_to c.name, c }.join(', ').html_safe}
    %p Key: #{@application.key}
  .small-8.large-8.columns
    %p Values:
    = render 'values/values_table', values: @application.values unless @application.values.empty?
= link_to '<i class="foundicon-edit"></i> Edit'.html_safe, edit_application_path(@application), class: 'main-button'

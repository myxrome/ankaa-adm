namespace :deploy do

  desc 'Exceute db seed task'
  task :seed do
    on roles(:db) do
      within release_path do
        with rails_env: :production do
          execute :rake, 'db:seed'
        end
      end
    end
  end

end